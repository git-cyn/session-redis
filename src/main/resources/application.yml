#redis
spring:
  redis:
    host: 127.0.0.1
    port: 6379
    password: 123456
    #超时一定要大于0
    timeout: 3000
  pool:
    max-idle: 8
    min-idle: 0
    max-active: 8
    max-wait: -1
  session:
    store-type: redis
